# Cloudflare Pages redirects and rewrites

# Serve static files directly (JSON, CSS, JS, etc.)
/data.json  /data.json  200
/pow3r.status.config.json  /pow3r.status.config.json  200
/assets/*  /assets/:splat  200

# API proxy (if needed)
/api/*  https://your-api-backend.com/:splat  200

# SPA fallback - all other routes go to index.html
/*    /index.html   200

